#ifndef MORSE_HELPERS_DECORATORS_STEP
#define MORSE_HELPERS_DECORATORS_STEP

void MorseStepDecorator(const Module::ProcessArgs &args, int key, Morse::MorsePattern &pattern, Morse::MorseStep &step)
{

  // // advance the pattern indexer it its not actie
  // if (this->patternAdvance == true)
  // {
  //   if (!pattern.isActive())
  //   {
  //     this->currentPattern++;
  //   }
  // }
}

#endif